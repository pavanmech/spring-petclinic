name: hello_job

on:
  push:
   branches:
     - master

jobs:
  build:

    runs-on: ubuntu-latest

    steps:

    - uses: actions/checkout@v1

    - name: Run a maven package
      run: mvn clean package
  

